<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=<device-width>, initial-scale=1.0">
    <title>Document</title>
</head>

<body>
    <form method="POST" action="/task/<%= task_id %>/assign">
        <input type="number" placeholder="maximum-points" name="maximum_points" min="1" max="500" required>
        <input type="datetime-local" placeholder="deadline" name="deadline" required>
        <input type="text" name="status" value="assigned" hidden readonly>
        <select name="worker_id">
            <% workers.forEach((worker)=>{ %>
                <option value="<%= worker._id %>"><%= worker.name %></option>
            <% }) %>
        </select>
        <button>Assign</button>
    </form>
</body>

</html>